version: "0.1.0"
description: AI導入アドバイザリーテンプレ辞書層スキーマ
sections:
  classifications:
    description: 導入リスク階層・支援フェーズ分類など
    files:
      - risk-levels.yaml
  thresholds:
    description: 判定閾値
    files:
      - intervention-thresholds.yaml
  references:
    description: 参照モデル（Markdown）
    files:
      - program-design.md
edit_policy:
  who: tenant_admin
  frequency: yearly
  read_only_for_ai: true
